<h1>Crime reporter information:</h1>

<p>reporter name:<%=@crime_reporter.reporter%></p>

reported crimes:
<%@crime_reporter.crimes.each do |crime|%>
  <ul>
    <li><%=link_to crime.crime_type, crime_path(crime)%></li>
  </ul>
<% end %>

<h2>
  <%=link_to 'Edit reporter', edit_crime_reporter_path(@crime_reporter)%> |
  <%=link_to 'Delete reporter', crime_reporter_path(@crime_reporter), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
</h2>
